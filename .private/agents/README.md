# Private Agent Overlays (`.private/agents/`)

Place private, operator-specific AI agent overlays here.

## Expected Files
* `{agent_name}_overlay.md` or `{agent_name}.md` (e.g. `torque_overlay.md` or `torque.md`).

These files are gitignored and will not be committed to version control.
